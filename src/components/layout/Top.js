import styled from 'styled-components';

const Top = styled.div`
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: #ffffff;
  font-size: 26px;
  font-weight: 700;

  ion-icon {
    font-size: 36px;

    :hover {
      cursor: pointer;
    }
  }
`;

export default Top;
